module evaluate/evaluate

imports
  nabl2shared
  nabl2runtime

imports
  desugar/desugar
  evaluate/-
  
strategies
  evaluate-all = bottomup(evaluate-exp);
                  // Note: try usage: 
                  // If there is nothing to format, this will fail. 
                  // Doesn't matter if it does.
                  try(alltd(format-num)) 

  evaluate = bottomup(evaluate-exp)
