module evaluate/evaluate

imports
  desugar/desugar
  evaluate/expression
  evaluate/number
  
strategies
  evaluate-all = desugar-all; 
                  bottomup(evaluate-exp);
                  // Note: try usage: 
                  // If there is nothing to format, this will fail. 
                  // Doesn't matter if it does.
                  try(alltd(debug; format-num)) 


