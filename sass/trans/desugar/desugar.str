module desugar/desugar

imports 
  desugar/-
  desugar/expression/-

/**
 * Desugaring
 */
strategies

  desugar-all = desugar-declarations
                  ; desugar-expressions
                  ; desugar-selectors
                  ; desugar-calls
  desugar-all-test = desugar-all; strip-annos

/**
 * Resugaring
 */
strategies
  resugar-all = resugar-selectors
                  ; resugar-expressions
  